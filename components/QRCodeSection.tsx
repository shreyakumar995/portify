// components/QRCodeSection.tsx
// This component is ONLY visible when printing — hidden on screen

'use client';

import { QRCodeSVG } from 'qrcode.react';

type Props = {
  username: string;
};

export default function QRCodeSection({ username }: Props) {
  // Build the full live URL
  
  // Before deployment use your Vercel URL:
  const portfolioUrl = `https://portify-git.vercel.app/u/${username}`;
  // const portfolioUrl = `https://your-project.vercel.app/u/${username}`;

  return (
    <div className="qr-section">

      {/* Divider line */}
      <div style={{
        borderTop: '1px solid #e5e7eb',
        marginTop: '2rem',
        paddingTop: '1.5rem',
        display: 'flex',
        alignItems: 'center',
        justifyContent: 'space-between',
        gap: '1.5rem',
      }}>

        {/* Left — text */}
        <div>
          <p style={{
            fontSize: '13px',
            fontWeight: 600,
            color: '#111',
            margin: '0 0 4px',
          }}>
            View live portfolio
          </p>
          <p style={{
            fontSize: '11px',
            color: '#6b7280',
            margin: '0 0 4px',
          }}>
            Scan to open the interactive version
          </p>
          <p style={{
            fontSize: '11px',
            color: '#6b7280',
            margin: 0,
            fontFamily: 'monospace',
          }}>
            {portfolioUrl}
          </p>
        </div>

        {/* Right — QR code */}
        <div style={{
          padding: '8px',
          border: '1px solid #e5e7eb',
          borderRadius: '8px',
          background: 'white',
          flexShrink: 0,
        }}>
          <QRCodeSVG
            value={portfolioUrl}
            size={90}
            bgColor="#ffffff"
            fgColor="#000000"
            level="M"           // M = medium error correction
            includeMargin={false}
          />
        </div>

      </div>

      {/* Generated by Portify note */}
      <p style={{
        fontSize: '10px',
        color: '#9ca3af',
        textAlign: 'center',
        marginTop: '1rem',
      }}>
        Generated by Portify — portify.dev
      </p>

    </div>
  );
}